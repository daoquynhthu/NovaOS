[package]
name = "libnova"
version = "0.1.0"
edition = "2021"
authors = ["NovaOS Team"]
description = "Core library for NovaOS native system calls and abstractions"

[lints]
workspace = true

[features]
default = ["nova_debug_console"]
std = ["sel4-sys/std"]
nova_debug_console = []

[dependencies]
sel4-sys = { path = "../seL4-sys", default-features = false }
spin = { workspace = true }
